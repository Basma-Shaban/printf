#include "main.h"

/**
 * File: printf_percent.c
 * Authers: Basma Shaban Ali and
 *
 * printf_percent - Prints the '%' character.
 * @val: va_list for variadic arguments (not used in this function).
 * Return: Always returns 1.
 */
int printf_percent(va_list val)
{
    _putchar('%');
    return (1);
}
